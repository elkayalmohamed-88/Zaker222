{
  "name": "azkari-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "أذكاري - Capacitor Android wrapper",
  "scripts": {
    "build": "echo Web app is static; Capacitor sync is handled by workflow",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}